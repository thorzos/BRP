logging:
  file.name: ./log/backend-test.log

spring:
  datasource:
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
  jpa:
    hibernate:
      ddl-auto: create

chat:
  upload:
    base-path: ./chat-images
    base-url: /api/v1/chats/uploads